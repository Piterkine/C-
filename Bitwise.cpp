#include <iostream>
using namespace std;
// ####################### Bitwise Operators ##################

int main()
{

          // Bitwise AND operator(&)
          // int a = 6, b = 10;
          // cout << (a & b) << endl;

          // Bitwise OR operator(|)
          // int a = 5, b = 7;
          // cout << (a | b) << endl;

          // Bitwise XOR operator(^)
          // int a = 3, b = 7;
          // 3=  0 1 1
          // 7=  1 1 1
          // ^ = 1 0 0 (4)
          // cout << (a ^ b) << endl;

          // Bitwise left shift
          // int a = 10;
          // cout << (a << 2) << endl;
          // O/P=40

          // Bitwise right shift
          // int b = 10;
          // cout << (b >> 1) << endl;
          // O/P=5

          // ######################## Operator Precedence #########################

          // (!,+,-) (unary Operators)   R to L (First)
          //(*,/,%)                      L to R (Second)
          //(+,-)                        L to R (Third)
          //(<,<=,>=,>)                  L to R (Fourth)
          //(==,!=)                      L to R (Fifth)
          //(&&)                         L to R (sixth)
          //(||)                         L to R (seventh)
          //(=)                          R to L (Last)

          // ################## Data Type Modifier #################

          // 1) long >= 4byte (Use only  integer and double to increase the stored size)
          // 2) short >= 2byte (Use only  integer and double to decrease the stored size)
          // 3) long long >= 4byte (Use only  integer and double to 2time increase the stored size)
          // 4) signed >= 4byte (Use only  integer and double to always stored positive and negative value)
          // 5) unsigned >= 4byte (Use only  integer and double to always stored positive value)
          // cout << sizeof(unsigned) << endl;
          // return 0;

          // Question 1) Figure out how to find the number is power of 2
          // Question 2) Figure out to reverse an integer number.
}